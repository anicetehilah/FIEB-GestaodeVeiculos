App As appinfo:
    BackEnabled: =false
    OnStart: =ClearCollect(colCameraImages,Defaults(VistoriaCarros).Anexos);
    Theme: =PowerAppsTheme

    Host As hostControl.DefaultHostControlVariant:
        OnCancel: =false
        OnEdit: =false
        OnNew: =false
        OnSave: =false
        OnView: =false

